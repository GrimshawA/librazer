import std

App: class {
	window: Window

	function main() {
    	window.open();

    	while (window.running())
    	{
    		window.poll();

    		window.drawRect(20, 20, 50, 50);

    		window.display();
    	}
    }
}
